*__receive.csv


1. Создать файл [Название функции]__receive.csv
2. Проверить кодировку (должна быть UTF-8 w/o BOM)
3. Выделить все входящие параметры и вставить в этот файл, заменить все точки с запятой (;) на пробел
4. Курсор в самое начало, Ctrl+H  
 search mode : Regular Expression . matches new line --Replace All надо будет нажимать много раз - пока есть результат замены)
 Find what :           \r\nТип: ([a-zA-Z]+)\r\n
 Replace With :        ;\1;
5. Заменить все ;string -> ;String (Ctrl+H , Search Mode: Normal)
6. Удалить, если есть

Web сервис EDBOGuides v 0.29
[номер страницы]
2012

7. Если описание продолжается после переноса строки , то нужно этот перенос убрать 
(описание должно быть на одной строке)

В конце поставить перенос строки
Курсор в самое начало, Ctrl+H  
 search mode : Regular Expression . matches new line --Replace All надо будет нажимать много раз - пока есть результат замены)
 Wrap around
 Find what :           \r\n([^;\r\n]+[\r\n])
 Replace With :        [пробел]\1

8. Проверить, чтобы соблюдался формат [название_параметра];[String|int];[Описание параметра - текст]\r\n
9. Сохранить файл
===============================================



*__return.csv


1. Создать файл [Название функции]__return.csv
2. Проверить кодировку (должна быть UTF-8 w/o BOM)
3. Выделить все параметры "Возвращаемое значение" и вставить в этот файл, заменить все точки с запятой (;) на пробел
4. Замена
Курсор в самое начало, Ctrl+H  
 search mode : Regular Expression
 Wrap around
 Find what :           ^([a-zA-Z_0-9]+)\s+([a-zA-Z_0-9]+)\s+(.+)$
 Replace With :        \1;\2;\3
5. Замена
Курсор в самое начало, Ctrl+H  
 search mode : Regular Expression . matches new line --Replace All надо будет нажимать много раз - пока есть результат замены)
 Wrap around
 Find what :           \r\n([^;\r\n]+)\r\n 
 Replace With :        [пробел]\1\r\n
6. Проверить, чтобы соблюдался формат [название_параметра];[тип];[Описание параметра - текст]\r\n
7. Сохранить файл


=================

Вписываешь в список название функции, для которой проделано все выше описаное
Список сохранить

